/* Driver header now delegates contract to BSP contract header. */
#pragma once

#include "bsp/bsp_require_drv_hal.h"

/* Driver implementations may add driver-private APIs here, but the public
 * driver contract is declared in `bsp_require_drv_hal.h`.
 */
